---
title: Hexo blog 搭建过程
date: 2017-12-19 19:52:07
tags:
---

本文主要记录了 Hexo blog 搭建过程

说明： 
$ 表示普通用户权限 
\# 表示注释<!-- more -->

```bash
# 以下两个命令都可以，
$ curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash
or
$ wget -qO- https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash

# 设置 nvm 镜像，你懂得...
$ echo "export NVM_NODEJS_ORG_MIRROR='https://npm.taobao.org/mirrors/node'" >> ~/.bashrc
$ echo "export NVM_IOJS_ORG_MIRROR='https://npm.taobao.org/mirrors/iojs'" >> ~/.bashrc

# nvm 生效，并安装 npm
# 也可以关闭终端，另外再开一个
$ source  ~/.bashrc
$ nvm install stable

# 设置 npm 镜像，可以通过以下命令查看
# 1. cat ~/.npmrc
# 2. npm get metrics-registry
# 3. npm config list
$ npm set metrics-registry='https://npm.taobao.org/'

# 开始安装 hexo
$ npm install -g hexo-cli
$ hexo init blog
$ cd blog
$ hexo generate
$ hexo server

# deploy
$ npm install hexo-deployer-git
```
